import "../css/Header.css";

const Header = ({ title }) => {

  return (
    <>
      <header>
        <h1>{title}</h1>
      </header>
      {/* <Outlet/> */}
    </>
  );
};

export default Header;
